// Bundle package: the plugins live in the export subpaths
// (startup/server/static/auth/api); the root entry names the bundle
// for clarity and for tests that import the package root.
export const name = "dsh-balbes-host";
